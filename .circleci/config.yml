version: 2.1
jobs:
  compile:
   docker:
      - image: fadlyas/kernel_dockerfile:latest
   steps:
      - run:
          no_output_timeout: 50m 
          command: |
            ls
workflows:
  version: 2.1
  cooking:
    jobs:
      - compile
